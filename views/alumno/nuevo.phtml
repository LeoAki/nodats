<div class="row-fluid">
    <div class="span12">
        <div class="widget-box">
            <div class="widget-title">
                <span class="icon">
                    <i class="icon-align-justify"></i>									
                </span>
                <h5><?= isset($this->titulo)?$this->titulo:''; ?></h5>
            </div>
            <div class="widget-content nopadding">
                <form class="form-horizontal" method="post" action="<?= BASE_URL; ?>alumno/grabar" name="form" id="form" novalidate="novalidate">
                    <div class="control-group">
                        <label class="control-label">Sección</label>
                        <div class="controls">
                        <select name="nivel" id="id_nivel">
                        <option value=""> ---NIVEL--- </option>
                        <?php foreach ($this->niveles as $nivel): ?>
                        <option value="<?php echo $nivel->nivel; ?>"> <?php echo $nivel->nomnivel; ?> </option>                        
                        <?php endforeach; ?>
                        </select>
                        <select name="grado" id="id_grado">
                        <option value=""> ---GRADO--- </option>
                        <?php foreach ($this->grados as $grado): ?>
                        <option value="<?php echo $grado->codigo; ?>"> <?php echo $grado->grado; ?> </option>                        
                        <?php endforeach; ?>
                        </select>                       
                        </div>
                        <?php echo $this->form; ?>
                        
                    </div>
                    <div class="form-actions">
                        <input type="submit" value="Grabar" class="btn btn-primary">
                    </div>
                </form>
            </div>
        </div>			
    </div>
</div>